# ── Local dev (recommended) ─────────────────────────────────────────────
# Copy .env.development values, or run: npm run dev (loads .env.development)
#
# VITE_USE_LOCAL_PROXY=true   → browser uses /api and /auth (Vite proxy, no CORS)
# VITE_API_TARGET=http://localhost:5000   → proxy target only (not sent to browser)
# Leave VITE_BACKEND_URL and VITE_API_URL empty for local proxy mode.
#
# Backend (Nolt-Admin-backend/.env):
#   PORT=5000
#   FRONTEND_URL=http://localhost:3000
#   GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback
#
# Start backend:  cd Nolt-Admin-backend && npm run dev
# Start frontend: cd Nolt-Admin-frontend && npm run dev

VITE_USE_LOCAL_PROXY=true
VITE_API_TARGET=http://localhost:5000
VITE_BACKEND_URL=
VITE_API_URL=

# ── Production (Amplify) ──────────────────────────────────────────────────
# Same-origin proxy: Amplify rewrites /api and /auth → APIM gateway.
# amplify.yml sets VITE_USE_LOCAL_PROXY=true at build time.
# In Amplify Console: set VITE_USE_LOCAL_PROXY=true and delete VITE_BACKEND_URL
# and VITE_API_URL (do not leave the APIM URL — cross-origin breaks cookies).
#
# APIM backend target for rewrites: https://apim.noltfinance.com/api/admin

# Optional
# GEMINI_API_KEY=
# VITE_PAYSTACK_PUBLIC_KEY=pk_test_...
